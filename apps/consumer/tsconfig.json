{
  "//": "Typechecks the queue consumer. Its sources are plain ESM JavaScript, so checkJs is what makes the gate meaningful; strict mode is off because the worker is untyped by design.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowJs": true,
    "checkJs": true,
    "strict": false,
    "noEmit": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "types": ["@cloudflare/workers-types", "node"]
  },
  "include": ["src/**/*.js"],
  "exclude": ["src/**/*.test.js"]
}
