# Agent policy pointer

The canonical repository policy is the root [AGENTS.md](../AGENTS.md).
The v7 pro-max pack in this directory supplies the lifecycle skills (`.ai/skills`),
the always-on rules (`AI_CODING_RULES.md` = root `AI_RULES.md`), the task protocol
(`AI_WORKFLOW.md` = root `TASK_PROTOCOL.md`) and the verification contract
(`AI_VERIFICATION.md` = root `VERIFICATION.md`).

Routing: start at `.ai/skills/using-skills/SKILL.md` and load only the skills it selects.
